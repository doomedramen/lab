pre-commit:
  parallel: true
  commands:
    go-vet:
      root: "apps/api"
      run: go vet ./...
      glob: "*.go"
    web-types:
      root: "apps/web"
      run: pnpm check-types
      glob: "*.{ts,tsx}"
    bump-versions:
      run: node scripts/bump-versions.js && git add VERSION apps/web/package.json apps/api/package.json package.json
      glob: "**/*.{ts,tsx,js,go}"

pre-push:
  parallel: false
  commands:
    # Local tests (uncomment if you want tests to run locally)
#    go-tests:
#      root: "apps/api"
#      run: go test ./...
#      glob: "*.go"
#    web-types:
#      root: "apps/web"
#      run: pnpm check-types
#      glob: "*.{ts,tsx}"

    # Docker-based tests (uncomment to use Docker CI for pre-push)
#    docker-tests:
#      run: docker-compose -f docker-compose.ci.yml test
#      fail_text: "Docker tests failed. Run 'docker-compose -f docker-compose.ci.yml test' to debug."
#      only_when: "files(glob='*.{go,ts,tsx,json,yaml,yml}') have changed"
