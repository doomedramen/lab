Ideas for features for this project: 
* add built in reverse proxy (simmilar to Nginx Proxy Manager) with https and certbot support. It could auto add endpoints based on vm/container name, e.g. domain *.rtin.page, container=debian = debian.rtin.page
* mobile first UI. 
* support for something like ansible playbook or infra as code.
* cluster support; cluster mounts (so mounts are duplicated between all nodes in a cluster)
* 
