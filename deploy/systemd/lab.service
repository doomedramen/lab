[Unit]
Description=Lab Virtualization Platform
Documentation=https://github.com/doomedramen/lab/blob/main/DEPLOYMENT_SYSTEMD.md
After=network.target libvirtd.service docker.service
Requires=libvirtd.service
Wants=docker.service

[Service]
Type=exec
User=lab
Group=libvirt

# Working directory
WorkingDirectory=/opt/lab

# Main executable (single binary with embedded web UI)
ExecStart=/opt/lab/api/lab-server
ExecReload=/bin/kill -HUP $MAINPID

# Environment
EnvironmentFile=-/etc/lab/lab-api.env
Environment=LAB_CONFIG=/etc/lab/config.yaml
Environment=LAB_ENV=production

# Restart policy
Restart=always
RestartSec=5
StartLimitBurst=3
StartLimitInterval=60s

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=lab

# Security hardening
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=read-only
PrivateTmp=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
ReadWritePaths=/var/lib/lab

# Resource limits
LimitNOFILE=65536
LimitNPROC=4096

[Install]
WantedBy=multi-user.target
